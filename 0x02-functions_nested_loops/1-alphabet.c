#include <main.h>
/**
 * print_alphabet - Function to print out lower case alphabets
 * Return: Returns zero for success
 */
void print_alphabet(void)
{
	int ch;

	for (i = 'a'; i <= 'z'; ch++)
	{
		_putchar(ch);
	}
	_putchar('\n');
}
